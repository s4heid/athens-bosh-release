<%=
  config = {
    'processes' => [{
        'name' => 'athens',
        'executable' => '/var/vcap/jobs/athens/bin/exec',
        'persistent_disk' => true
      },
    ]
  }
  YAML.dump(config)
%>
