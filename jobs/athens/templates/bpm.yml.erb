<%=
  config = {
    'processes' => [{
        'name' => 'athens',
        'executable' => '/var/vcap/jobs/athens/bin/run-athens',
        'persistent_disk' => true
      },
    ]
  }
  YAML.dump(config)
%>
