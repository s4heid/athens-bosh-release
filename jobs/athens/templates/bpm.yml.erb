<%=
  config = {
    'processes' => [{
        'name' => 'athens',
        'executable' => '/var/vcap/packages/athens/bin/athens',
        'args' => ['-config_file', '/var/vcap/jobs/athens/config/athens.toml'],
      },
    ]
  }
  YAML.dump(config)
%>
